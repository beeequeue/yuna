query EpisodeList($id: Int!, $provider: Provider!) {
  episodes: Episodes(id: $id, provider: $provider) @client {
    provider
    id
    animeId
    title
    duration
    progress
    index
    episodeNumber
    url
    subtitles
    thumbnail
    isWatched
  }
}


