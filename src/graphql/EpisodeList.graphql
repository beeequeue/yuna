query EpisodeList($id: Int!) {
  episodes: Episodes(id: $id, provider: CRUNCHYROLL) @client {
    provider
    id
    animeId
    title
    duration
    progress
    index
    episodeNumber
    isWatched
    url
    thumbnail
  }
}


