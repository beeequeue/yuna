query AnimeQueueQuery($id: Int) {
  anime: Media(id: $id, type: ANIME) {
    id
    idMal
    title {
      userPreferred
    }
    episodes
    status
    siteUrl
    bannerImage
    nextAiringEpisode {
      airingAt
      timeUntilAiring
    }
    mediaListEntry {
      id
      progress
      status
      repeat
      score
    }
    relations {
      edges {
        relationType
        node {
          id
          title {
            userPreferred
          }
          bannerImage
        }
      }
    }
  }
}
